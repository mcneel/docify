name: Heroku build and deploy

on:
  push:
    branches: [ 'main' ]
  pull_request:
    branches: [ 'main' ]
  workflow_dispatch:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: install and build
      run: |
        cd quasar_site/
        yarn
        yarn ssrbuild

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - run: pwd
    - name: init git repo and deploy
      run: |
        cd quasar_site/dist/ssr
        git init
        git remote add heroku https://heroku:fae6583a-d8af-4745-9865-ef9e6e61a8a3@git.heroku.com/mcneel-apidocs.git
        git push heroku master --force
